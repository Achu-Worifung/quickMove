# build using pip install pynput

from pynput.mouse import Button, Controller

mouse = Controller()

def on_click(x, y)

if __name__ == '__main__':
    while(True):
        # works for both two screens 
        print(f'the mouse position is {mouse.position}')
    
    